#include "SumN.h"
#include <bits/stdc++.h>
#include <iostream> 

using namespace std; //표준 네임스페이스 사용


long long sum(vector<int>& a)
{
	long long result = 0;

	for (int i = 0; i < a.size(); i++) {
		result += a[i];
	}
	//cout << result;
	return 0;
}

int main() {

}