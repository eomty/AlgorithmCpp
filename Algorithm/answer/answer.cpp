#include "answer.h"
#include <bits/stdc++.h>

using namespace std;

vector<int> _array;  //
int a; //공포도
int i;// 반복 횟수
int result; //결과
int count; //모험가 수

int main() {
	cin >> a;  //공포도

	for (i = 0; i < a; i++) {
		int x;
		cin >> x;
	//	_array.벡터 함수? sort오름차순 정렬? 인구수 - 공포도=0이상?
	}
	cout << a;
}